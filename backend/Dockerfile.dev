# Development image with hot reload (air). Source is bind-mounted.
FROM golang:1.26-alpine
WORKDIR /app

RUN apk add --no-cache git

COPY go.mod go.sum ./
RUN go mod download

COPY . .

EXPOSE 8080 2222
CMD ["go", "tool", "air"]
